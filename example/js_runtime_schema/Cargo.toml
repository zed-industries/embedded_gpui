[package]
name = "js_runtime_schema"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
description = "The interface the js_runtime component's root object implements"

[lints]
workspace = true

[lib]
path = "src/js_runtime_schema.rs"

[dependencies]
embedded_gpui = { path = "../../embedded_gpui" }
gpui = { git = "https://github.com/zed-industries/zed.git", branch = "gpui-embedded-in-gpui", default-features = false }
