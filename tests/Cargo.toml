[package]
name = "tests"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
description = "Integration tests for embedded_gpui (with their wasm guest fixture in test_plugin/)"

[lints]
workspace = true

[[test]]
name = "objects"
path = "objects.rs"

[[test]]
name = "js_runtime"
path = "js_runtime.rs"

[dev-dependencies]
anyhow = "1.0"
embedded_gpui = { path = "../embedded_gpui" }
embedded_gpui_util = { path = "../embedded_gpui_util" }
test_schema = { path = "schema" }
embedded_gpui_js = { path = "../embedded_gpui_js" }
futures = "0.3"
serde = { version = "1.0", features = ["derive"] }
gpui = { git = "https://github.com/zed-industries/zed.git", branch = "gpui-embedded-in-gpui", features = ["test-support"] }
rand = "0.9"
